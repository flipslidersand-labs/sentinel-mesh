[package]
name = "sentinel-agent-ebpf"
version = "0.1.0"
edition = "2021"

# Build with:
#   cargo +nightly build --target bpfel-unknown-none -Z build-std=core
# Requires: bpf-linker, LLVM dev headers (sudo apt install llvm-18-dev)

[dependencies]
aya-ebpf = "0.2"

[[bin]]
name = "sentinel-agent-ebpf"
path = "src/main.rs"

[workspace]
